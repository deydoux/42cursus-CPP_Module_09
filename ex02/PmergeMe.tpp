#ifndef PMERGEME_TPP
#define PMERGEME_TPP

#include "PmergeMe.hpp"

template <typename C>
C PmergeMe::algorithm(C container) {
	size_t size = container.size();
	if (size < 2)
		return (container);

	return (container);
}

#endif
